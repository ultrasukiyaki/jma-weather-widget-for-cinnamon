# v3.0.0-alpha.2.2

## 修正内容

地域設定を保存しても天気表示が従来の地域から変わらない問題を修正しました。

原因は、外部設定画面が旧設定ディレクトリ`~/.cinnamon/configs`へ書き込む一方、現在のCinnamonが`~/.config/cinnamon/spices`を参照していたことです。Cinnamon本体と同じ設定パス解決ルールへ変更し、保存後はD-Bus経由で実行中アプレットへ再読込を通知します。

旧設定ファイルしか存在しない環境では、互換性のため旧パスを引き続き使用します。
