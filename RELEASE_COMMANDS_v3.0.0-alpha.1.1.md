# v3.0.0-alpha.1.1 Release Commands

```bash
./test.sh
git add .
git commit -m "fix: load local modules with Cinnamon CJS importer"
git tag -a v3.0.0-alpha.1.1 -m "JMA Weather Widget v3.0.0-alpha.1.1"
git push origin HEAD --tags
```
