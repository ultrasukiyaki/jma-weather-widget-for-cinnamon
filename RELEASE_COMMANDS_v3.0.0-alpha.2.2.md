# v3.0.0-alpha.2.2 release commands

```bash
./test.sh
git add .
git commit -m "fix: apply external location settings to the active applet"
git tag -a v3.0.0-alpha.2.2 -m "v3.0.0-alpha.2.2"
git push origin HEAD --tags
```
