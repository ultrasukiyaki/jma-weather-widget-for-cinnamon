# v3.0.0-alpha.2.3 release commands

```bash
./test.sh

git add .
git commit -m "fix: close settings window after successful save"

git tag -a v3.0.0-alpha.2.3 -m "v3.0.0-alpha.2.3"
git push origin HEAD
git push origin v3.0.0-alpha.2.3
```
